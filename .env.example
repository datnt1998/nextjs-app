# =============================================================================
# NextJS Starter Kit - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these values from your Supabase project dashboard:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the Project URL and anon/public key
#
# Documentation: https://supabase.com/docs/guides/getting-started

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Your Supabase anonymous/public key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Your Supabase service role key (KEEP SECRET - server-side only)
# Only needed for admin operations and server-side API routes
# WARNING: This key bypasses Row Level Security - never expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# ImageKit Configuration (REQUIRED for image uploads)
# -----------------------------------------------------------------------------
# Get these values from your ImageKit dashboard:
# 1. Go to https://imagekit.io/dashboard
# 2. Go to Developer Options > API Keys
# 3. Copy your Public Key, URL Endpoint, and Private Key
#
# Documentation: https://docs.imagekit.io/api-reference/api-introduction

# Your ImageKit public key (safe to expose in browser)
NEXT_PUBLIC_IMAGEKIT_PUBLIC_KEY=your-public-key

# Your ImageKit URL endpoint (e.g., https://ik.imagekit.io/your-id)
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your-id

# Your ImageKit private key (KEEP SECRET - server-side only)
# Used for generating authentication signatures for uploads
# WARNING: Never expose this key to the client
IMAGEKIT_PRIVATE_KEY=your-private-key

# -----------------------------------------------------------------------------
# Site Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# The base URL of your application
# Used for generating absolute URLs, redirects, and metadata
# 
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Additional Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Add any additional environment variables your application needs below
# Examples:
# - Analytics keys (Google Analytics, Plausible, etc.)
# - Payment provider keys (Stripe, PayPal, etc.)
# - Email service keys (SendGrid, Resend, etc.)
# - Feature flags
# - API rate limits
# -----------------------------------------------------------------------------
